# 실험 로그 (olist)

## Day 0 — 환경 셋팅 (2026-09-19)
- 목적: 분석을 시작할 수 있는 프로젝트 환경을 만들고 GitHub에 안전하게 올릴 준비
- 한 일:
  - data-analysis-portfolio 저장소 안에 olist 폴더 생성, 폴더 구조(data/raw·interim·processed, notebooks, src, docs, outputs) 구성
  - kagglehub로 받은 CSV 9개를 data/raw에 복사
  - .gitignore에 olist/data/raw 등 추가 후 git check-ignore로 무시 확인
  - Python 3.13.9 가상환경(.venv) 생성, 라이브러리 설치(pandas 3.0.6, numpy 2.5.3, scipy 1.18.1, scikit-learn 1.9.1, duckdb 1.5.5 등), requirements.txt 저장
  - .vscode/settings.json으로 노트북 기준 폴더를 olist로 고정
  - 01_load_check.ipynb에서 orders shape (99441, 8) 확인
- 결과: 환경 정상 동작, 첫 커밋 완료
- 문제·시행착오:
  - .venv 활성화 시 PowerShell "스크립트를 실행할 수 없습니다" 오류 → Set-ExecutionPolicy -Scope CurrentUser RemoteSigned로 해결
  - CSV 복사 때 kagglehub 캐시 폴더 구조(olistbr/brazilian-ecommerce/versions/2)가 통째로 raw 안에 들어감 → CSV 9개만 raw 바로 아래로 옮기고 빈 폴더 삭제
  - settings.json을 만들려다 VS Code 전체 개인 설정(User settings.json)을 열어버림 → 프로젝트 전용 파일(olist/.vscode/settings.json, Workspace Settings)과 구분해서 새로 만듦
- 내일: DuckDB로 CSV 9개 등록, 행 수·결측 확인


## Day 1 — 적재·품질 확인 (2026-09-19 ~ 09-20)
- 목적: CSV 9개를 DuckDB에 등록하고 행 수·결측을 확인, 계획서(부록 B) 기준 수치를 내 SQL로 재현
- 한 일: 01_load_check.ipynb (DuckDB VIEW 등록 → 행 수 → 타입 → 결측 → 도착일 결측 원인 → 리뷰 중복 제거 → 지연율·저평점 재현). 마지막에 Restart & Run All로 재현 확인(실행 번호 1~17 연속, 에러 없음), 노트북 끝에 con.close() 추가. 코드 리뷰 지적 사항을 검증하는 셀 5개 추가(멀티셀러 분석 모집단 기준, 분석기간 내 delivered·canceled, 리뷰 중복, 기타, 결정 문서의 나머지 사실)
- 결과:
  - 행 수: orders 99,441 / order_items 112,650 / order_reviews 99,224 / order_payments 103,886 / customers 99,441 / sellers 3,095 / products 32,951 / geolocation 1,000,163 / cat_tr 71 (전부 일치)
  - 결측: orders(승인 시각 160건, 출고일 1,783건, 도착일 2,965건=3.0%), products(카테고리 등 610건=1.9%, 무게·크기 각 2건), order_reviews(제목 88.3%, 코멘트 58.7%). 그 외 테이블과 geolocation은 결측 없음
  - 우편번호 타입: customers·sellers·geolocation 모두 INTEGER
  - 부록 B 재현 10개 전부 일치:
    - 리뷰 중복 제거 후 98,673행 (초과 551행 제거)
    - 분석기간 주문 99,092 / 도착일 있는 주문 96,204 / 지연율 6.79%
    - 도착+리뷰 존재 95,561 / 저평점 12.8% / 코멘트 있음 40.5%
    - 저평점률: 지연 주문 62.4%, 비지연 주문 9.2%
    - 3단계 대상(지연 아님 & 저평점) 8,247건, 그중 코멘트 있음 6,449건
    - 멀티셀러 주문: order_items 전체 기준 1,278건(98,666건의 1.3%) / 분석 모집단(96,204건) 기준 1,272건(1.32%)
  - 도착일이 있는 분석 모집단 96,204건 중 리뷰가 없는 주문 643건 (chk에는 리뷰 있는 95,561건만 포함)
  - (2026-09-22 재검토 추가) base(96,204건) 안에서 order_approved_at 결측 14건. orders 테이블 전체 기준 결측 160건과는 다른 수치(모집단이 다름)
- 해석:
  - 도착일 결측 2,965건은 대부분 배송 중·취소·미가용 등 배송 미완료 주문. 예외로 delivered인데 도착일 없음 8건, canceled인데 도착일 있음 6건(전체 기간). 이 6건 중 분석기간 내는 1건뿐(나머지 5건은 2016-10 주문). 이 1건(2018-02-19 주문)은 late=1(지연)로 계산됨(2026-09-22 확인, decisions.md D-00)
  - 지연 주문의 저평점률(62.4%)이 비지연(9.2%)의 약 6.8배로, 지연과 저평점은 강하게 연결됨
    - **(2026-09-22 재검토로 보정)** 이 62.4%/9.2% 비교는 리뷰 응답 시점을 통제하지 않은 단순 비교임. 지연 주문 리뷰의 68.5%가 도착일 이전에 응답됐고(아직 오지 않았다는 불만이 섞였을 가능성), 도착 후 응답한 지연 주문만 보면 저평점률은 19.5%로 비지연(9.2%)의 2.1배로 줄어듦. "6.8배" 자체를 결론으로 인용하지 말 것 — decisions.md D-00c 참고
  - 그러나 전체 저평점 12,228건 중 지연 주문은 3,981건(약 33%)뿐. 지연을 완벽히 예측해도 저평점 주문의 약 67.4%(8,247건)는 비지연 주문에서 발생 → 3단계(리뷰 텍스트 분석)의 필요성을 숫자로 확인
    - **(2026-09-22 표현 정정)** "저평점의 약 2/3는 지연과 무관하게 남음"이라는 원래 표현은 인과관계를 함축해 부정확함. 비지연 주문에도 배송과 관련된 다른 불만(파손·오배송 등)이 섞여 있을 수 있으므로 "지연과 무관"이 아니라 "비지연 주문에서 발생"이 정확한 서술
  - 3단계 대상 8,247건 중 78%(6,449건)에 코멘트가 있어 텍스트 분석 자료는 충분
  - 저평점 비율 12.8%는 계획서의 14.7%(리뷰 테이블 전체 기준)와 모집단이 달라서 다른 값. 2단계 기저율은 12.8% 사용
  - **(2026-09-22 재검토 추가)** comment_pct 확정치: chk(2단계 모집단, 95,561건) 기준 40.51%를 최종값으로 사용. 계획서의 40.6%는 재현되지 않음(리뷰 dedup 후 분석기간 무관 전체 기준 41.32%와도 불일치) — 출처 불명한 초기 추정치로 보고, 계획서에는 각주로 정정해둘 것
  - **(2026-09-22 재검토 추가)** chk(리뷰 있는 주문만) 기반 통계는 선택편향을 안고 있음: 리뷰 없는 643건의 지연율 23.8% vs 리뷰 있는 95,561건의 지연율 6.7%로, 지연된 주문일수록 리뷰를 덜 남기는 경향이 있을 수 있음. "배송완료 주문 전체"로 일반화하지 말 것
  - **(2026-09-22 재검토 추가)** 리뷰 재평가로 점수가 달라지는 경우(order_id 중복 547건 중 202건)가 있고, 그중 low 라벨이 최신/최초 응답 기준에 따라 실제로 뒤집히는 주문이 96건(0.1%). 현재 규칙(최신 응답 우선)은 유지하되 한계로 기록
- 코드 리뷰에서 나온 것(→ decisions.md 반영): 취소 주문 1건 처리(D-00), review_id 중복(D-00d), 우편번호 앞자리 0(D-00e), payment_installments 0값 2건(D-06), 마스터 테이블 구조(D-07), 모집단 정의 단일화(D-08), DuckDB 연결 관리(E-07)
- **(2026-09-22) Day1 작업물을 여러 AI에게 교차 검토받고 나온 보정 사항 → decisions.md D-00·D-00b·D-00c 반영**: canceled 1건 late=1 확인, 리뷰 재평가로 인한 low 라벨 플립 96건, 리뷰 유무 선택편향, 리뷰 응답 시점 편향("6.8배"→"2.1배" 보정), comment_pct 확정(40.51%), 2016년/2018-09~10월 제외 근거 수치(329건/20건). 01_load_check.ipynb에는 PK 유일성 검증 셀 추가(orders/customers/sellers/products/order_items 복합키/cat_tr), 검증5(b) 쿼리에 late 계산 추가, 결론 셀 표현 수정
- 문제·시행착오:
  - 결측 확인 함수(null_profile)가 결측 없는 테이블에서 "Empty DataFrame..."을 출력 → 빈 표 판단을 len(res)로 바꿔 "결측 없음"이 나오도록 수정 (숫자는 처음부터 정상)
  - 사전 대응: 우편번호가 CSV에서 따옴표로 감싸져 문자열로 읽히므로 read_csv에 types=INTEGER를 지정 (조인 키 타입 통일). 이 과정에서 앞자리 0이 사라지는 것은 조인에 무해하지만 알아둘 점(D-00e)
  - 멀티셀러 1.3%가 전체 기간 기준이라 분석 모집단 기준(1,272건)과 분모가 다름 → 주석과 기록에서 구분
  - DuckDB 파일 락: 다른 노트북 커널이 같은 파일을 열고 있으면 읽기 전용으로도 열리지 않음 → 노트북 끝에 con.close(), 새 노트북 전에 이전 커널 종료
  - 다른 AI 코드 리뷰 7건 중 일부는 DuckDB의 NULL 정렬을 잘못 가정(DESC에서 NULL이 앞으로 온다고 지적, 실제로는 NULLS LAST) → 지적 내용은 데이터로 확인한 뒤에 반영
- 내일(Day 2): Day 1 커널 종료 후 시작 → base 테이블(96,204행, 지연율 6.79%) → geolocation 진단(DESCRIBE, 위·경도 min/max, 유일 우편번호 개수) → 브라질 범위 밖 좌표 처리 → geo_rep → distance_km

## Day 2 — geolocation 대표좌표·distance_km (2026-09-21)
- 목적: 우편번호별 대표좌표를 만들고 고객·셀러 사이 거리(distance_km)를 계산. 모집단 기준 테이블(base)도 생성
- 한 일: 02_geo_distance.ipynb (base → geolocation 진단 → 범위 밖 좌표 제거 → 평균 vs 중앙값 근거 → geo_rep → 매칭률 4방향 분해 → item_lvl/distance_km → 거리 분포 검증). Restart & Run All 완료(실행 번호 1~14 연속, 에러 없음), 노트북 끝에 con.close()
- 결과:
  - base: 96,204행(주문 유일), 지연 6,532건(6.79%)
  - geolocation: 1,000,163행, 유일 우편번호 19,015개, 우편번호당 평균 52.6행(중앙값 29, 최대 1,146), 1행짜리 1,043개. 위도 -36.6~45.1, 경도 -101.5~121.1로 브라질 밖 좌표가 섞여 있음
  - 범위 밖 좌표: 31행(20개 우편번호, 11개 주)을 제거해 geo_clean 1,000,132행·우편번호 19,011개. 노로냐 섬 11행은 정상이라 포함 (D-09)
  - 평균 vs 중앙값: 대표점이 10km 넘게 다른 우편번호 223개, 50km 넘게 96개. 평균에서 200km 넘는 좌표가 섞인 우편번호 207개(최대 2,994km) → 중앙값 채택 (D-10)
  - geo_rep: 19,011행(우편번호당 1행), n=1인 우편번호 1,042개
  - 매칭률(미매칭/전체): 고객 우편번호 158/14,994(A 157, B 1), 행 279/99,441, base 주문 265/96,204(0.28%). 셀러 우편번호 7/2,246(A 7), 행 7/3,095, base 주문 217/96,204(0.23%). 거리 계산 불가 base 주문 481건(0.5%) (D-04)
  - item_lvl: 112,650행(조인 전후 동일), distance_km NULL 555건. 거리 최소 0 / 25% 183 / 중앙값 431.7 / 75% 792 / 90% 1,451 / 최대 3,579km, 4,500km 초과 0건. 상파울루↔리우 360.7km (D-11)
  - 셀러 지역: 셀러 수 기준 SP 59.7%, 아이템 기준 SP 71.3%
- 해석:
  - 오류 좌표는 31행뿐이지만 우편번호 평균을 크게 흔들 수 있어 제거가 필요. 다만 박스 필터로는 브라질 안의 오류 좌표를 못 걸러서(207개 우편번호) 대표좌표를 중앙값으로 잡음
  - 미매칭은 거의 전부 geolocation에 우편번호가 없는 경우라 범위 필터 기준과 무관. 미매칭 셀러 7곳이 217개 주문(0.23%)에 영향을 줌. 셀러별 주문 수는 127 / 45 / 34 / 6 / 2 / 2 / 1건으로 3곳이 95%를 차지하고, 최대 셀러(127건)는 전체 셀러 2,945곳 중 상위 5.4%. 셀러당 평균(31건 vs 전체 33건)만 보면 평범해 보이지만 분포가 심하게 치우쳐(전체 중앙값 7건) 오해할 수 있음. 초대형 셀러(최대 1,819건)는 아니고 영향은 작음
  - 거리 분포는 긴 꼬리(중앙값 432km, 90% 1,451km, 최대 3,579km). 셀러는 상파울루에 집중돼 있고, SP의 아이템 비중(71.3%)이 셀러 비중(59.7%)보다 커서 SP 셀러가 평균적으로 더 많이 팖
- 문제·시행착오:
  - 범위 필터의 경도 상한을 -34로 잡으면 브라질 영토인 노로냐 섬 11행이 잘림 → -28로 수정 (초기 SQL의 값이 틀렸음)
  - 이 노트북에서 만든 적 없는 geo_rep·item_lvl·base가 DuckDB 파일에 이미 있었음(이전 시도의 흔적) → 모든 테이블을 CREATE OR REPLACE로 만들어 재실행해도 같은 결과가 나오게 함 (E-08)
  - 거리 이상 신호 기준을 "수천 km"에서 4,500km 초과로 수정(정상 최대가 3,579km) (D-11)
  - 미매칭 셀러 7곳을 평균 주문 수(31건 vs 33건)만 비교해 "평범한 셀러"라고 해석했다가, 셀러별 분포를 보니 한 곳이 59%를 차지하고 전체 중앙값이 7건인 치우친 분포라 해석을 정정 → 평균만으로 판단하지 말고 분포를 함께 볼 것
  - 범위 밖 31행은 도시명이 브라질 지명(porto trombetas, santa rosa 등)인데 좌표는 포르투갈·스페인 쪽, 멕시코·미국 남부, 아르헨티나, 이탈리아 등 해외로 보임 → 이름이 같은 해외 지명으로 잘못 지오코딩된 것으로 추정(별도 확인, 노트북에는 도시명 출력 없음)
- 내일(Day 3): 주문 단위 마스터 테이블(base 기준 LEFT JOIN + low NULL, D-07) → 대표 아이템 기준 카테고리·셀러·거리, 가격·배송비 합, 결제 회차 최대 → 검증(order_id 유일성, 행 수 흐름). D-05 멀티셀러 일치율(모집단 기준), D-06 installments 0 처리

## Day 3 — 주문 단위 마스터 테이블 (2026-09-21)
- 목적: 아이템 단위 표를 주문 1행으로 접고(대표 아이템 규칙), 리뷰를 LEFT JOIN해 모델링용 마스터 테이블을 만든다
- 한 일: 03_master.ipynb (입력 점검 → item_base → 대표 아이템 2종 → 합산·결제 → ord_agg → D-05 → D-12 근거 → D-13 → master → 요약·D-06 → assert → parquet 저장 → 동점·결제 규칙 민감도(보충)). Restart & Run All 완료(실행 번호 1~14 연속, 에러 없음, assert 통과), 노트북 끝에 con.close()
- 결과:
  - item_base: 109,873행(item_lvl 중 base 소속), 주문 96,204. 카테고리 미상 아이템 1,557(원본 결측 1,535 + 번역표에 없는 22)
  - 대표 아이템 2종·합산·ord_agg: 모두 96,204행(주문 유일), 결제 표 99,440행, 멀티셀러 1,272건, base 주문 중 결제 없는 주문 0
  - D-05: 멀티셀러 1,272건 중 셀러 기준 623건(49.0%), 아이템 기준 615건(48.3%), 8건은 다른 아이템·같은 셀러
  - D-12: 아이템이 전부 거리 NULL인 주문 477건(멀티셀러 6), 일부만 NULL인 주문 4건(전부 멀티셀러)
  - D-13: 가격 최대 동점 7,431주문(카테고리 다른 동점 56), 동점 방향을 뒤집으면 카테고리 변경 53건, 미상 1,376 → 1,379
  - 민감도(보충): 동점 규칙을 뒤집으면 D-05 셀러 기준 일치율 46.5~49.0%(591~623건), 거리 동점으로 seller_rep이 바뀌는 주문 143건(0.15%, distance_km은 불변), 결제 행 2개 이상인 base 주문 2,865건 중 회차가 다른 주문 845건(2,865건의 29.5%, base 전체의 0.88%)
  - master: 96,204행(주문 유일), 26개 컬럼. 지연 6,532건(6.79%), 리뷰 있음 95,561 / 없음 643, 저평점 12,228건(리뷰 있는 주문 기준 12.8%), 거리 없음 477, 카테고리 미상 주문 1,376
  - 3단계 대상(지연 아님 & 저평점) 8,247건, 코멘트 있음 6,449건 (Day 1과 같은 값)
  - 결제 회차 범위 0~24, 0값 2건(D-06). 가격 합 13,179,807.94 / 배송비 합 2,191,977.24 / 아이템 수 109,873(item_base와 일치)
  - 행 수 흐름: 99,441 → 99,092(분석기간) → 96,204(base) → 96,204(master) → 95,561(리뷰 있음)
  - master.parquet: 96,204행(주문 유일), 15.4MB (data/processed, git 제외)
- 해석:
  - 마스터가 Day 1의 수치(지연 6.79%, 저평점 12.8%, 3단계 대상 8,247건)를 그대로 재현해서, 조인·집계 과정에서 행이 늘거나 사라지지 않았음을 확인
  - 멀티셀러 주문은 1.32%뿐이지만 그 절반 넘게(649건, 전체의 0.67%)가 카테고리 대표와 셀러·거리 대표가 다른 셀러를 가리킴 → 카테고리 중요도 해석의 한계로 기록(D-05)
  - 가격 동점 규칙(order_item_id 오름차순)이 결과에 미치는 영향은 미미(대표 카테고리 변경 53건, 0.06%)
  - 동점 규칙을 바꿔도 D-05의 결론(절반 가까이 불일치)은 유지되지만, seller_rep이 tie-break에 의존하는 주문이 143건(0.15%) 있어 Day 6의 seller_avg_delay 한계로 기록(D-12)
- 문제·시행착오:
  - dist_missing 검증을 "마스터 안에서 합과 NULL 개수 비교"로 하려다가, 정의상 항상 같아 확인이 안 되는 검사라서 item_base에서 따로 센 값(아이템이 전부 거리 NULL인 주문 수)과 비교하도록 바꿈
  - 계획의 컬럼 수를 24개로 잘못 적었다가 세어보니 25개였고, seller_state를 추가해 26개로 확정
  - 외부 리뷰의 "회귀용 delay_days 컬럼 추가" 제안은 승인된 계획서(회귀를 분류로 대체, estimated_delivery_days는 X 제외)와 달라 반영하지 않음. 배송오차·예상소요일은 Day 4 EDA에서 마스터의 날짜 컬럼으로 계산
  - 노트북 마지막의 con.close() 셀이 빠져 있던 것을 확인 과정에서 발견해 추가(E-07)
  - D-12에 seller_rep이 임의로 정해지는 주문을 6건(전부 NULL인 멀티셀러)으로만 적었다가, 리뷰에서 나온 동점 민감도 지적을 계산해 보니 거리 동점까지 포함하면 143건이라 정정
  - purchase_ts를 "보조"로 분류했던 것을 "분할 기준 전용, 모델 입력 금지"로 정정(data_dictionary.md). purchase_month는 연도가 빠지는 문제를 D-15로 등록
- 내일(Day 4): master.parquet을 pd.read_parquet로 읽어 EDA 시작. 먼저 orders 원본으로 분석기간 밖(2016년, 2018-09~10)까지 전체 월의 도착일 결측률을 확인(마지막 달 절단 편향 여부와 분석기간 경계의 근거)하고, D-03(테스트 기간)의 결정 규칙(월 범위·양성 수 기준)을 숫자로 먼저 기록한 뒤 월별 지연율·저평점률 추이 확인, Y 분포, 분석 기간 기준 상관계수 재확인(배송오차와 예상소요일·거리), 카테고리 미상 주문의 지연·저평점 비교, 가격 동점 주문(7,431건)의 원인 확인, distance_km의 유니크 값 수와 거리 동점(D-12)이 같은 우편번호 셀러에서 생기는지, 143건의 구성(동점 vs 전부 NULL)과 seller_state 동일 여부 확인
