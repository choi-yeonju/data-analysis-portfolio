# 결정 로그 (olist)

## 환경·운영 결정
- E-01 위치·구조: data-analysis-portfolio/olist 안에서 작업. 근거: 포트폴리오를 한 저장소에 모으고 프로젝트별 폴더로 분리
- E-02 데이터 관리: 원본 CSV는 olist/data/raw에 두고 git에는 올리지 않음(.gitignore에 raw·interim·processed 추가). 근거: geolocation이 약 60MB이고 GitHub 파일 크기 제한(100MB)·저장소 용량 문제. 재현 방법: kagglehub.dataset_download("olistbr/brazilian-ecommerce")
- E-03 DuckDB 파일 위치: data/interim/olist.duckdb (실행 계획 초안의 data/olist.duckdb에서 변경). 근거: interim은 .gitignore 대상이라 실수로 커밋되지 않음
- E-04 노트북 경로 기준: .vscode/settings.json의 jupyter.notebookFileRoot = ${workspaceFolder}, VS Code에서 olist 폴더를 열어 작업. 근거: notebooks/ 안에서도 data/raw/... 짧은 경로를 쓰기 위해
- E-05 실행 환경: Python 3.13.9, olist/.venv, 패키지 버전은 requirements.txt로 고정
- E-06 SQL 도구: DuckDB 사용(설치 없이 CSV를 SQL로 조회). 근거: 계획서의 SQL 조인 요건을 서버 세팅 부담 없이 충족. 참고: read_csv, EXCLUDE는 DuckDB 전용 문법
- E-07 DuckDB 연결 관리: 노트북 마지막에 con.close()를 실행하고, 새 노트북을 열기 전에 이전 노트북 커널을 종료. 근거: DuckDB 파일은 다른 프로세스(다른 노트북 커널)가 열고 있으면 읽기 전용으로도 열리지 않음("Could not set lock on file"). 같은 프로세스 안에서 다시 연결하는 것은 문제없음
- E-08 테이블 생성 방식: 노트북에서 만드는 테이블(base, geo_clean, geo_rep, zip_status, item_lvl 등)은 모두 CREATE OR REPLACE로 만들어 재실행해도 같은 결과가 나오게 함. 근거: DuckDB 파일에 이전 시도에서 만든 테이블이 남아 있을 수 있어(Day 2에 geo_rep·item_lvl·base가 미리 존재), 정의가 다를 수 있음
- E-09 마스터 저장: 주문 단위 마스터 테이블(master)을 data/processed/master.parquet에 저장(DuckDB COPY). 이 폴더는 .gitignore 대상이라 GitHub에는 올리지 않음. Day 4부터는 이 파일을 pandas(pd.read_parquet)로 읽어 EDA·통계·모델링을 진행 — DuckDB/SQL에서 pandas로 넘어가는 지점

## 분석 결정 (계획서 규칙의 구현 기준)
### D-00 분석 모집단
- 분석기간 2017-01-01 ~ 2018-08-31 구매 주문 중 도착일(order_delivered_customer_date)이 있는 주문 (96,204건)
- delivered인데 도착일 없는 8건: 분석기간 내에서도 8건 전부 해당(분석기간 내 delivered 96,211건 중 도착일 있음 96,203건). 도착일 필터로 제외
- canceled인데 도착일 있는 6건(전체 기간) 중 분석기간 내는 1건(2018-02-19 주문, 리뷰 3점). 나머지 5건은 2016-10 주문이라 기간에서 제외
- 정합성: 분석 모집단 96,204건 = delivered(도착일 있음) 96,203건 + canceled(도착일 있음) 1건
- 결정: 도착일 유무 기준을 그대로 유지해 그 1건은 포함. 근거: 도착일이 기록돼 있고 리뷰까지 있어 실제로 받은 주문으로 판단. 규칙이 단순하고 영향은 1건(96,204건 중)
- **확인(Day1 재검토): 이 1건(canceled+도착일 있음)은 late=1(지연)로 계산됨.** 도착일(order_delivered_customer_date)이 예상도착일(order_estimated_delivery_date)보다 늦어 late 정의(D-00c)에 그대로 해당. 별도 예외 처리 없이 base.late 계산 로직을 그대로 적용
- **분석기간 경계 근거(Day1 재검토, 원본 orders 기준): 2016년 주문 329건(표본 부족으로 제외), 2018-09~10월 주문 20건(전부 도착일 없음, 배송 지연 여부 계산 불가로 제외)**. 두 구간은 서로 다른 이유로 빠짐 — 2016년은 "표본 규모" 문제, 2018-09~10월은 "배송완료 정보 없음" 문제

### D-00b 리뷰 중복 처리 (주문 단위)
- 한 주문에 리뷰가 여러 개면 review_answer_timestamp가 가장 최근인 1건만 사용. 동률이면 review_creation_date 최신, 그다음 review_id 오름차순(ASC)
- NULL 정렬: DuckDB 기본이 NULLS LAST이고 review_answer_timestamp의 NULL은 0건이라 영향 없음. 다른 DB(PostgreSQL 등)로 옮길 때는 NULLS LAST를 명시할 것
- 결과: 99,224행 → 98,673행 (초과 551행 제거, 리뷰가 2개 이상인 주문은 547개)
- **이 규칙이 실제로 라벨을 바꾸는 정도(Day1 재검토): order_id 중복 547건 중 점수(review_score) 자체가 서로 다른 경우 202건(단순 재전송이 아니라 진짜 재평가). 이 중 최신 응답 대신 최초 응답을 썼다면 low(저평점 여부) 값이 달라졌을 주문이 96건(전체 96,204건의 0.1%). 규칙(최신 응답 우선)은 "최근 경험을 반영한다"는 논리로 유지하되, 이 96건은 저평점 정의의 한계로 최종 보고서에 명시**

### D-00c 지연·저평점 정의
- 지연(late) = 도착일(날짜) > 예상도착일(날짜). 시각은 버리고 날짜만 비교(CAST AS DATE)
- 저평점(low) = review_score <= 2. 리뷰가 없는 주문은 low를 0이 아니라 NULL로 둠(저평점 비율 왜곡 방지)
- 01_load_check의 chk 테이블은 2단계 확인용이라 INNER JOIN(리뷰 있는 95,561건만). 리뷰 없는 643건이 빠지므로 1단계 모집단(96,204건)으로 쓰지 않음. 마스터 테이블(Day 3)은 LEFT JOIN + low NULL로 따로 만듦
- 2단계 기저율은 모집단(분석기간·배송완료·리뷰 1건) 기준 12.8% 사용. 계획서의 14.7%는 리뷰 테이블 전체 기준
- **chk 기반 통계의 선택편향(Day1 재검토): 리뷰 없는 643건의 지연율은 23.8%, 리뷰 있는 95,561건의 지연율은 6.7%로, 리뷰 없는 주문에서 지연율이 3.5배 높음. 즉 chk(리뷰 있는 주문만)로 계산한 지표는 이미 "지연되면 리뷰를 덜 남기는 경향"이 섞인 표본이라, "배송완료 주문 전체"로 일반화하면 안 됨**
- **"지연 시 저평점률 6.8배"라는 표현의 보정(Day1 재검토): 지연 주문 리뷰의 68.5%가 도착일 이전에 응답됨(아직 오지 않았다는 불만이 섞였을 가능성이 큼). 도착 후 응답한 지연 주문만 보면 저평점률은 19.5%로, 비지연 주문(9.2%)의 2.1배 — 미조정 비교(62.4% vs 9.2%, 6.8배)보다 훨씬 완만함. "지연이 저평점을 6.8배 유발한다" 같은 인과적 표현은 쓰지 않고, "리뷰 응답 시점을 통제하지 않은 단순 비교"라는 단서를 반드시 병기**
- **표현 정정: "저평점의 약 67%는 지연과 무관" → "저평점 주문의 약 67.4%(8,247/12,228)는 비지연 주문에서 발생". "무관"은 인과관계를 함축하므로 지양. 비지연 주문에도 배송과 관련된 다른 불만(파손, 오배송 등)이 섞여 있을 수 있음 — 이것이 3단계(리뷰 텍스트 분석)가 필요한 이유**
- **comment_pct 확정치(Day1 재검토): chk(2단계 모집단, 95,561건) 기준 40.51%를 최종값으로 사용. 계획서의 40.6%는 재현되지 않음(리뷰 dedup 후 전체·분석기간 무관 기준 41.32%, dedup 전 원본 기준 41.30%와도 불일치) — 출처 불명한 초기 추정치로 간주하고 계획서에 각주로 정정**

### D-00d review_id 중복 (3단계용)
- 원본에서 같은 review_id가 서로 다른 order_id 여러 개에 붙어 있음: 789개 review_id (2개 주문 764개, 3개 주문 25개, 초과 814행). 점수·코멘트는 동일
- D-00b는 order_id 기준 dedup이라 이 중복은 남음: 주문 단위 dedup 후에도 561개 review_id가 1,139개 주문에 걸침
- 3단계 영향: 코멘트 있는 대상 6,449주문 → 고유 review_id 6,400건(약 0.8%). 지연율·저평점률 같은 주문 단위 수치에는 영향 없음
- 결정 예정: 3단계 표본 추출·키워드 집계는 review_id 기준으로 중복 제거 (분석 단위를 주문이 아니라 리뷰로)

### D-00e 우편번호 처리
- 원본은 항상 5자리 문자열이고 앞자리 0으로 시작하는 값이 많음(sellers 1,027행, customers 23,995행). read_csv에서 INTEGER로 지정하면 앞자리 0이 사라짐 (예: "01037" → 1037)
- customers·sellers·geolocation을 모두 INTEGER로 통일해 조인에는 문제없음. 위험한 건 문자열과 정수가 섞이는 경우
- 지도 시각화나 외부 우편번호 자료와 합칠 때만 LPAD(CAST(zip AS VARCHAR), 5, '0')로 복원

### D-05 멀티셀러 대표 아이템 기준 일치율 (완료, Day 3)
- 대상: 분석 모집단(96,204건) 중 멀티셀러 주문 1,272건(1.32%). 카테고리 = 가격이 가장 큰 아이템, 셀러·거리 = 거리가 가장 먼 아이템(계획서 규칙)
- 결과: 셀러 기준(가격 대표 아이템의 셀러 = 거리 대표 아이템의 셀러) 623건(49.0%), 아이템 기준(같은 아이템) 615건(48.3%). 8건은 서로 다른 아이템인데 같은 셀러
- 해석: 멀티셀러 주문의 절반 넘게(1,272 − 623 = 649건) 카테고리 대표와 셀러·거리 대표가 서로 다른 셀러의 아이템을 가리킴. 영향 범위는 전체 주문의 0.67%(649/96,204). 동점 규칙(가격·거리)을 뒤집으면 셀러 기준 일치율은 46.5~49.0%(591~623건)로 움직이지만 결론(절반 가까이 불일치)은 같음(03_master.ipynb [보충] (a)). 셀러 기준이 의미상 자연스러움(가격 대표 아이템이 거리 대표 셀러의 것인지가 핵심). 1단계 Feature Importance에서 카테고리 중요도를 해석할 때 "대표 아이템 선정 기준의 부작용일 수 있음"을 한계로 명시

### D-06 결제 회차 처리: 0값·MAX 집계 규칙 (완료, Day 3)
- 마스터에서 installments_max = 0인 주문 2건(credit_card, 두 주문 모두 결제 행이 그 한 건뿐). 값 범위 0~24
- 결정: 원본 그대로 유지. 근거: 영향 2건(0.002%), 보정(결측 처리나 1로 변경)은 계획서에 없는 가정을 추가함
- 출처: 결제 유형과 결제 행 수는 01_load_check.ipynb [검증 4]·[검증 5]에서 확인
- 주문 단위 결제 회차는 계획서 규칙대로 최댓값(MAX)으로 접음. 결제 행이 2개 이상인 base 주문 2,865건 중 회차가 행마다 다른 주문은 845건(2,865건의 29.5%, base 전체 96,204건의 0.88%)이라, 이 주문들에서는 MAX 규칙이 결과를 좌우함(03_master.ipynb [보충] (c))

### D-07 마스터 테이블 구조 (확정, Day 3)
- base 기준 LEFT JOIN rev + low NULL 단일 테이블. 96,204행(주문 유일), 26개 컬럼
- 1단계 모집단 = 96,204건 전체, 2단계 모집단 = low가 NULL이 아닌 95,561건(리뷰 없는 643건은 low NULL)
- late는 다시 계산하지 않고 base.late를 그대로 사용(D-08 정의 단일화). 컬럼 역할과 입력 금지 이유는 docs/data_dictionary.md
- 마스터에 회귀용 컬럼(delay_days 등)과 estimated_delivery_days는 넣지 않음: 승인된 계획서가 회귀를 분류로 대체했고 estimated_delivery_days는 X에서 제외. 상관계수 재확인용 배송오차·예상소요일은 Day 4 EDA에서 마스터의 날짜 컬럼으로 계산(EDA 전용, 모델 입력 금지)

### D-08 모집단 정의 단일화 (완료, Day 2)
- base 테이블에 분석 모집단(분석기간 + 도착일 있음)과 지연(late) 정의를 한 곳에 둠. 이후 모든 SQL은 base 기준으로 사용
- 검증: 96,204행(주문 유일), 지연 6,532건(6.79%)
- item_lvl은 order_items 전체(112,650행) 기준이라 base 밖 주문의 아이템도 포함. Day 3에서 base와 조인해 분석 모집단으로 한정
- Day 3 반영: item_base = item_lvl ∩ base (109,873행, 주문 96,204). base 밖 아이템 2,777건(112,650 − 109,873)은 제외

### D-09 범위 밖 좌표 제거 (geolocation)
- 브라질 범위 박스(위도 -34~6, 경도 -74~-28) 밖 좌표 31행(20개 우편번호)을 제거해 geo_clean 생성: 1,000,163행 → 1,000,132행. 유일 우편번호는 19,015개 → 19,011개(4개는 좌표가 전부 박스 밖)
- 제거된 좌표는 위도 45, 경도 121 등 브라질 밖 값이고 11개 주에 흩어져 있음
- 경도 상한을 -28로 잡은 이유: 페르난두 지 노로냐 섬(PE, 경도 약 -32.4, 11행)은 브라질 영토라 정상 좌표. 본토 동쪽 끝(약 -34.8)을 상한으로 쓰면 이 섬이 잘림

### D-10 대표좌표: 평균 → 중앙값 (계획서와 다른 점)
- 계획서는 우편번호별 위·경도 평균이지만, 우편번호별 중앙값(위도·경도 각각)으로 변경. 이상치 보정이 목적이고 구조를 바꾸는 결정은 아님. 다음 피드백 때 한 줄로 언급
- 근거(02_geo_distance.ipynb 셀 8): 박스 필터 후에도 평균 대표점과 중앙값 대표점이 10km 넘게 다른 우편번호 223개(전체 19,011개의 1.2%), 50km 넘게 96개(최대 1,120km). 평균에서 200km 넘게 떨어진 좌표가 섞인 우편번호 207개(최대 2,994km) → 브라질 안이지만 엉뚱한 곳에 찍힌 좌표가 평균을 끌어당김
- 위도·경도를 각각 중앙값으로 잡은 점이라, 진짜 기하학적 중앙값과는 미세하게 다를 수 있음(실용적 근사)
- geo_rep에 관측치 수 n을 함께 저장. n=1인 우편번호 1,042개는 중앙값도 그 1개 좌표라 걸러낼 수 없음(이를 쓰는 아이템 539건, 0.48%). 거리가 이상한 주문을 추적할 때 확인용

### D-11 거리 이상 신호 기준
- 아이템 단위 distance_km이 4,500km를 넘으면 이상 신호로 봄(브라질 본토 안 최장 직선거리가 대략 4,300km대). "수천 km" 같은 기준은 쓰지 않음(정상 값도 3,500km대까지 나옴)
- 결과: 최소 0 / 중앙값 431.7 / 90% 1,451 / 최대 3,579km, 4,500km 초과 0건. 공식 검증: 상파울루↔리우 360.7km

### D-12 대표 거리 정책 (Day 3)
- 규칙: 아이템 중 거리가 가장 먼 아이템을 대표로 삼되, NULL 거리는 후보에서 제외(NULLS LAST), 동점은 order_item_id 오름차순
- 근거(계획서의 order 단위 집계 정책 요지): 지연의 원인은 "어느 아이템이 비쌌는가"가 아니라 "어느 셀러가 멀리서·느리게 보냈는가"이므로, 가격 기준(카테고리)과 다르게 최장 거리 아이템의 셀러를 대표로 지정. 다만 거리가 가장 먼 셀러가 실제로 가장 늦게 보낸 셀러라는 보장은 없는 가정임
- 현황: 아이템이 전부 거리 NULL인 주문 477건(그중 멀티셀러 6건), 일부만 NULL인 주문 4건(전부 멀티셀러). 477 + 4 = 481은 Day 2의 "거리 계산 불가 481건"과 일치
- 전부 NULL인 477건만 distance_km이 NULL이고 dist_missing = 1. 일부만 NULL인 4건은 있는 값 중 최대를 써서 dist_missing = 0
- 전부 NULL인 멀티셀러 6건은 seller_rep이 order_item_id 오름차순으로 정해져 사실상 임의. 나머지 471건은 셀러가 1명이라 무관
- 동점이 생기는 이유(논리적 설명): distance_km이 우편번호 단위 대표좌표로 계산돼, 같은 우편번호의 셀러는 같은 고객에 대해 거리가 동일해짐. Day 4 EDA에서 동점 후보 셀러가 같은 우편번호인지, distance_km의 유니크 값 수는 얼마인지 확인
- 거리 동점(서로 다른 셀러가 같은 최대 거리)까지 포함하면, 동점 방향을 뒤집을 때 seller_rep이 바뀌는 주문은 143건(0.15%)이고 distance_km은 변하지 않음(03_master.ipynb [보충] (b)). 전부 NULL인 멀티셀러 6건도 같은 tie-break에 의존하지만, 143건의 구성(같은 우편번호 동점 vs 전부 NULL)과 동점 셀러의 seller_state 동일 여부는 Day 4 EDA에서 노트북으로 확인. Day 6의 seller_avg_delay는 이 주문들에서 tie-break 규칙(order_item_id 오름차순)에 따라 셀러가 정해진다는 점을 한계로 기록
- 검증: item_base에서 "아이템이 전부 거리 NULL인 주문 수" = master의 dist_missing 합 = 477 (독립 계산, assert 통과)

### D-13 동점 규칙과 민감도 (Day 3)
- 대표 아이템의 가격 동점은 order_item_id 오름차순으로 깸(재실행해도 같은 결과)
- seller_rep(거리 동점)과 D-05 일치율에 대한 민감도는 D-12, D-05 참고
- 민감도: 가격 최대 동점 주문 7,431건(7.7%), 그중 카테고리가 다른 동점 56건. 동점 방향을 뒤집으면 대표 카테고리가 바뀌는 주문 53건(0.06%), '미상' 여부가 바뀌는 주문 9건(미상 1,376 → 1,379)

### D-14 카테고리 '미상' 처리 (Day 3)
- 계획서 규칙 구현: 카테고리가 비었거나 번역표에 없으면 '미상'. 원본 포르투갈어명은 category_pt로 보존
- 현황: 미상 아이템 1,557건(원본 결측 1,535 + 번역표에 없는 22), 대표 카테고리 기준 미상 주문 1,376건. 상위 N 카테고리 묶음(D-02)은 Day 5에 결정

## 확정 대기·실행 대기 (D-01~D-03은 Day 4~5 EDA 후 결정, D-15는 Day 5~6, D-04는 확인 완료·대체 실행은 Day 6)
- D-01 seller_avg_delay 산출 방식(누수 차단 포함, Day 5~6): 정의는 계획서 규칙(셀러별 과거 지연 비율, 학습 구간 기준). 이름은 seller_avg_delay(계획서 명칭)지만 값은 지연 일수가 아니라 지연 비율(0~1)이며, 코드에서도 정의를 함께 적음. Y(late)를 셀러 단위로 집계해 X로 쓰는 구조라 타깃 인코딩과 같은 누수 위험이 있으므로, 학습 행은 자기 자신을 뺀 값(leave-one-out)으로, 교차검증에서는 fold마다 그 fold의 학습 구간 기준으로 다시 계산하고, 테스트 구간에는 학습 구간 전체 통계를 적용. 최소 주문 건수(min_n)는 후보별 커버 셀러 수·주문 비율·추정 오차 표를 보고 결정하고, 미달 셀러는 학습 구간 전체 평균 + 결측 더미(계획서 규칙). 테스트 구간에만 나타나는 신규 셀러(학습 구간 0건)는 min_n 미달의 특수한 경우로 보고 같은 대체 규칙(학습 구간 전체 평균 + 결측 더미)을 적용하되, 학습 구간 기준으로 테스트 구간 주문 중 신규 셀러가 몇 %인지 Day 5에 확인. 소표본 스무딩은 계획서에 없는 추가라, 도입하면 계획 변경으로 기록. **Day1 재검토에서 leave-one-out 자체의 누수 위험이 다시 지적됨 — Day5~6에서 out-of-fold 또는 시간순 expanding 방식과 비교 후 확정할 것(테스트 성능으로 고르지 않음)**
- D-02 카테고리 상위 N: 누적 커버율 표 후 결정
- D-03 테스트 기간: 월별 지연율·저평점률 추이 확인 후 결정 (결정 후 변경 금지). 결정 규칙(예: 마지막 3개월, 양성 수가 부족하면 연장)은 월별 추이를 보기 전에 이 문서에 먼저 기록해서, 그래프를 보고 유리한 구간을 고르는 일이 없게 함. 규칙에는 실제 월 범위와 양성 수 기준(건수)까지 숫자로 적음
- D-04 geolocation 미매칭 처리 (Day 2 확인 완료, 대체 실행은 Day 6): 미매칭은 NULL로 두고 비중만 기록. 대체(동일 주 평균 + 결측 더미)는 학습 구간이 정해진 뒤 학습 데이터로만 계산. 현황: 고객 쪽 base 주문 265건(0.28%), 셀러 쪽 217건(0.23%, 셀러 7곳 중 3곳이 95%), 거리 계산 불가 주문 481건(0.5%). 미매칭은 거의 전부 geolocation에 우편번호 자체가 없는 경우(A)이고, 범위 필터로 제거된 경우(B)는 고객 1건뿐. item_lvl에서 distance_km NULL 555건(0.49%). geolocation 1,000,163행, 결측 없음(Day 1). **대체 기준 키가 미정: 고객 쪽은 customer_state로 하면 되지만 셀러 쪽(217건) 대체 키가 data_dictionary.md에 없음 — Day 6 전에 정할 것**
- D-15 purchase_month 처리(Day 5~6): MONTH()라 연도가 빠져 1~8월은 2017·2018이 섞이고 9~12월은 2017뿐. 계절성과 추세가 섞이므로 정수 / 범주형 / 미사용 중 결정
- D-16 모델 입력 화이트리스트 (Day 6): 사후 정보 누수를 막기 위해 STAGE1_FEATURES / STAGE2_FEATURES 목록을 코드로 고정하고, 입력 금지 컬럼(docs/data_dictionary.md)과 겹치지 않는지 assert로 확인. 목록에는 seller_rep 자체가 아니라 그로부터 파생한 seller_avg_delay(+결측 더미)를 넣음
